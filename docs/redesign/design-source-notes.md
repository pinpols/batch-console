# 设计源与实现侧记录(实现方维护;design/ 目录只读,不在其中做任何改动)

## 权威设计源
- `design/Batch Console 重设计-离线版.html`(2026-07-04 12:52 交付包主件,全站+组件总览+响应式动效规格;深链锚点见 design/交付说明.md)+ `design/全局质感合集.html`(Toast/空态/骨架)
- 旧版(单屏 .dc.html / 设计定稿 / 旧离线版)已由用户确认删除(2026-07-04)。
- 参考:`design/export/batch-console-设计规格.md`(文字规格)、`design/refs/`(token/关键页参考图)。

## v2 覆盖核查(2026-07-04)
- 已覆盖:13 nav 页;抽屉三态(查看/编辑/新建);确认弹窗;告警分组+静默 24h;Worker 负载卡;文件汇总卡。
- v2 未画(用母版语言套,不再请设计补):报表中心 / 自助服务 / 调度快照 / 触发器 / Trace / 工作流运行与设计器 / 执行日志 / 系统管理次级页。
- 可选补充(不阻塞):Toast / 空态 / 加载骨架合集图。

## 实现侧与设计的数据口径差(用户已拍板)
- 文件列表汇总卡:设计「已归档 / 迟到」无后端字段 → 实现用「已处理(LOADED)/ 失败(FAILED)」,形态照设计。
- Worker 负载 6/16:后端无容量分母字段 → 实现显示真实 currentLoad 数字,不编造进度条分母。
- 侧栏默认展开态(用户拍板),v2 原型默认收起图标轨。
