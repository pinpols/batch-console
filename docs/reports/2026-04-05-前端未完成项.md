# 前端未完成项清单

整理时间：`2026-04-05`

目的：

- 只保留当前仍未完成、仍需推进的事项
- 已修复、已落地、仅做结论确认的内容不再重复记录

---

## 1. 后端仍需补齐

### 1.1 系统管理接口缺失

当前仍缺：

- tenant 管理接口
- user 管理接口
- role 管理接口
- permission 管理接口

影响：

- [TenantList.vue](/Users/dengchao/Downloads/batch-console/src/views/system/TenantList.vue)
- [UserRole.vue](/Users/dengchao/Downloads/batch-console/src/views/system/UserRole.vue)

当前这两页只能做成本地上下文页 / 只读页，无法实现真正的系统管理 CRUD。

### 1.2 高频列表缺少服务端筛选能力

当前多个页面仍依赖：

- 前端拉全量
- 本地过滤
- 本地分页

典型影响页面：

- [JobDefinitionList.vue](/Users/dengchao/Downloads/batch-console/src/views/job/JobDefinitionList.vue)
- [WorkflowDefinitionList.vue](/Users/dengchao/Downloads/batch-console/src/views/job/WorkflowDefinitionList.vue)
- [FileTemplateList.vue](/Users/dengchao/Downloads/batch-console/src/views/file-center/FileTemplateList.vue)
- [DispatchChannel.vue](/Users/dengchao/Downloads/batch-console/src/views/worker/DispatchChannel.vue)
- [AuditList.vue](/Users/dengchao/Downloads/batch-console/src/views/observability/AuditList.vue)
- [JobInstanceList.vue](/Users/dengchao/Downloads/batch-console/src/views/monitor/JobInstanceList.vue)

建议后端补：

- query filter 参数
- 保持 `PageResponse`
- 避免前端继续全量聚合

### 1.3 Worker 辅助接口未闭环

遗留项：

- `takeover` 能力未保留
- `channelReceipts` 仍待后端 OpenAPI / 前端正式封装

### 1.4 实时观测协议未落地

当前仍缺：

- SSE / 实时日志 / 执行轨迹标准端点

现阶段仍以 query + 轮询为主。

### 1.5 告警路由 / 通知策略 OpenAPI 未明确

当前仍缺：

- 告警路由 / 通知策略相关 Excel 或配置接口契约

---

## 2. Workflow 设计器未完成项

### 2.1 提交流程异常分支仍需明确化

文件：

- [WorkflowDesigner.vue](/Users/dengchao/Downloads/batch-console/src/views/workflow/WorkflowDesigner.vue)

待做：

- 为 `submitToBackend` 增加显式 `catch`
- 区分：
  - 提交失败
  - 提交成功但重新加载失败

### 2.2 本地草稿结构校验仍缺

文件：

- [WorkflowDesigner.vue](/Users/dengchao/Downloads/batch-console/src/views/workflow/WorkflowDesigner.vue)

待做：

- `readDraft()` 在 `JSON.parse` 成功后补基础结构校验
- 结构过期时提示并回退到后端数据

### 2.3 `loadWorkflow` 的竞态检查可继续前移

文件：

- [WorkflowDesigner.vue](/Users/dengchao/Downloads/batch-console/src/views/workflow/WorkflowDesigner.vue)

待做：

- 在关键 `await` 返回后立即做 token 校验
- 进一步压缩过期请求参与状态更新的可能性

### 2.4 校验逻辑需统一使用业务字段

文件：

- [WorkflowDesigner.vue](/Users/dengchao/Downloads/batch-console/src/views/workflow/WorkflowDesigner.vue)

待做：

- 统一基于 `fromNodeCode / toNodeCode`
- 避免混用 X6 内部 `cell.id`

### 2.5 设计器配置生命周期仍未真正闭环

当前还缺：

- 发布
- 下线
- 版本对比
- 草稿与已发布版本 diff
- 回滚入口

### 2.6 编辑效率能力仍缺

当前还缺：

- 撤销 / 重做
- 框选 / 多选 / 批量移动
- 复制 / 粘贴子图
- 对齐 / 分布 / 吸附辅助线
- 快捷键帮助面板
- 节点搜索与定位

### 2.7 前置语义约束仍不完整

当前建议继续补：

- 连线时禁止成环
- `DECISION` 默认边唯一
- `CONDITION` 边必须有条件表达式
- `START` 不允许入边
- `END` 不允许出边
- `JOIN` 明确 join 模式
- 关联 job / pipeline / worker / window 做存在性校验

### 2.8 大图治理能力仍不足

当前还缺：

- 节点分组 / 子流程折叠
- 泳道 / 逻辑分区
- 上下游链路高亮
- 大图搜索 / 筛选 / 视图保存

### 2.9 运行态辅助仍不足

当前还缺：

- 运行实例叠加视图
- 最近一次运行结果着色
- 节点耗时 / 重试 / 失败次数 overlay
- 从节点跳转到实例 / 日志 / 审计 / 文件
- dry-run / 模拟执行 / 影响分析

### 2.10 协作与安全保护仍不足

当前还缺：

- 删除节点 / 连线确认
- 离开页面未保存提醒
- 草稿与后端版本冲突提示
- 编辑锁 / 最近编辑人 / 更新时间
- 操作审计

### 2.11 读取模式在大租户下仍有性能瓶颈

当前问题：

- 先拉全租户 `workflow-nodes` / `workflow-edges`
- 再前端按 `workflowDefinitionId` 过滤

建议：

- 后端提供按 `workflowDefinitionId` 或 `workflowCode` 精确查询接口

### 2.12 缩略图语义问题仍未根治

当前问题：

- 仍偏“内容 fit 概览”
- 不是稳定的“大画布视口指示器”

建议方向：

- 引入 `Scroller`
- 或自己维护固定 world bounds

---

## 3. 优先级建议

### 立即推进

1. Workflow 设计器：
   - 草稿结构校验
   - 竞态检查前移
   - 校验逻辑统一使用业务字段
2. 高频列表后端筛选参数
3. 高频列表的服务端筛选参数补齐

### 高优先级

1. `submitToBackend` 显式 `catch`
2. 系统管理接口（tenant / user / role / permission）

### 中优先级

1. `fetchAllPageItems` 支持 `AbortSignal`
2. `getData()` 类型守卫收敛

### 低优先级

1. 空画布提示
2. `undo/redo`
3. 路由入口收敛
