import DefaultTheme from 'vitepress/theme'
import './custom.css'

/**
 * 自定义 VitePress 主题:扩展默认主题,额外注入 CSS 变量对齐控制台品牌色。
 * 入口位置 .vitepress/theme/index.ts 是 VitePress 约定。
 */
export default DefaultTheme
